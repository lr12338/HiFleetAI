# 当前开发状态

## 1. 当前阶段

- 当前仓库路径：`/home/ecs-user/HiFleetAI`
- 当前主开发分支：`feature/P0-05-pytest`
- 当前阶段判断：Phase 2 已完成收口
- 当前附加状态：本地完整体验闭环已补齐，可继续规划 Phase 3

## 2. 已完成阶段

### Phase 0：工程底座

已完成：

- 配置系统
- Docker Compose 模板
- 数据库模型与迁移基础
- pytest 基础设施
- Harness 骨架

### Phase 1：Agent 核心链路

已完成：

- 健康检查
- 会话与消息基础模型
- Model Gateway 抽象
- Supervisor 最小规则路由
- Harness 基础回归链路

### Phase 2：标准客服后台

已完成：

- 登录与鉴权
- 会话列表
- 会话详情
- 人工接管
- AI 暂停 / 恢复
- 内部备注
- Harness 结果页
- `/api/v1/chat` MVP 契约

### Phase 2 后续补强：本地体验闭环

已完成：

- 前端 `/api` 本地代理
- 本地管理员初始化脚本
- `/api/v1/chat` best-effort 落库
- Ark 文本模型调用
- 前端 `Test Chat` 页面

## 3. 当前已交付能力

### 后端

- `auth`
- `chat`
- `conversations`
- `harness`
- `health`

### 前端

- 登录页
- 受保护路由
- 会话列表与详情
- 接管 / 暂停 / 恢复
- 内部备注
- Harness 结果页
- Test Chat 页面

### 测试与质量

- 后端 `pytest` 全量通过
- 前端 `npm test` 与 `npm run build` 通过
- Harness `handoff` 与 `regression` 通过

## 4. 当前未完成能力

以下能力尚未真正接入：

- 微信公众号真实接入
- Chatwoot 真实接入
- 微信客服平台真实接入
- 真实网页检索执行
- 真实多模态应用内推理
- 真实生产 Skill 服务接入
- 知识库导入 / 索引 / 检索闭环
- 渠道消息同步与来源展示

## 5. 当前关键事实

根据最近一轮系统验证，当前必须明确以下事实：

- 本地 SQLite 路径已验证可用
- `admin / admin` 在本地测试环境可登录
- `/api/v1/chat` 已可用，并支持 FAQ / Skill / regression / general_chat 最小链路
- Ark 文本模型链路已部分打通
- 多模态仅完成 Ark 直连验证，未接入应用内 `/api/v1/chat`
- `HiFleetData/` 当前仍未接入真实知识检索链路

## 6. 当前环境阻塞

当前主要阻塞不在业务代码，而在环境层：

- Docker Compose 自举受 Docker daemon 权限限制
- PostgreSQL 应用内验证受 `psycopg` 运行时依赖阻塞
- MinIO 未在最近一轮系统验证中成功启动

## 7. 当前风险

- 如果误命中旧端口服务，容易误判当前工作树可用
- `harness/reports/latest.*` 会被覆盖，不能当作稳定交付物
- `HiFleetData/` 为本地未跟踪目录，不能误提交
- `.env` 与真实密钥只允许本地使用

## 8. 建议的下一步

建议不要直接进入 Phase 3 编码，而是先做三件事：

1. 补齐 PostgreSQL 驱动与 Docker 权限，重跑基础设施级验证
2. 明确知识库导入/索引/检索方案
3. 基于渠道统一契约拆解 Phase 3 任务，而不是直接堆实现

## 9. 本文档职责

本文档只负责回答：

- 当前做到哪
- 已完成什么
- 未完成什么
- 当前有哪些环境与能力阻塞
- 建议从哪里继续开发
