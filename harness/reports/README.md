# Harness Reports

Generated reports are written here during eval runs.

Git ignores generated report files in this directory. Keep this README tracked so the directory exists in fresh clones.

P0-06 writes:

- `latest.json`: structured run metadata, summary, and per-case failures.
- `latest.md`: human-readable summary of the latest run.
