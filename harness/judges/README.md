# Harness Judges

Deterministic judges are required before any LLM judge is introduced.

Structural assertions must verify tool calls, retrieval logs, handoff state, response shape, and failure reasons without relying on subjective model scoring.
