# Harness Cases

JSONL case files live here. Each line is one schema-validated case loaded by
`harness/runners/run_eval.py`.

Initial categories:

- `faq`
- `rag`
- `skill`
- `handoff`
- `regression`

No case may encode fake success or hard-coded model output.
